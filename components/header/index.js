import React from "react";
import { AppBar } from "./styles";

export default function Header() {
  return (
    <AppBar>
      <label style={{ fontSize: "20px" }}>Infinite Scroll Example</label>
    </AppBar>
  );
}
