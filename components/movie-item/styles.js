import styled from "styled-components";
import { XS, SM, MD, LG, XL } from "../../styles/global.styles";

export const MovieItem = styled.div`
  height: inherit;
  display: flex;
  justify-content: center;
  flex-direction: column;
  text-align: center;
  color: #fff;
`;
